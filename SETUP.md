# 天玥社區區權會回饋系統｜正式後端設定

## 已完成
- 75 戶正式戶號／門牌／驗證碼資料庫
- 前端不再包含全部 75 戶驗證碼
- 戶號＋驗證碼改由 Google Apps Script 後端驗證
- 驗證成功後核發 30 分鐘 Session Token
- 回覆寫入 Google Sheets「回覆資料」
- 同戶再次回覆時，舊紀錄標為 `superseded`，最新紀錄標為 `active`
- Google Sheets「統計」頁自動計算回覆率、出席意願、辦法立場與各棟回覆戶數

## 第 1 步：建立 Google Sheets 資料庫
1. 將 `天玥社區_區權會回覆管理資料庫.xlsx` 上傳 Google Drive。
2. 用 Google 試算表開啟，並轉成 Google 試算表格式。
3. 確認工作表名稱保持：`住戶代碼`、`回覆資料`、`統計`。

## 第 2 步：加入 Apps Script
1. 在該 Google 試算表開啟「擴充功能 → Apps Script」。
2. 刪除預設程式碼。
3. 將本資料夾 `Code.gs` 全部貼入並儲存。
4. 「部署 → 新增部署作業 → 類型：網頁應用程式」。
5. 執行身分：我。
6. 誰可以存取：任何人（Anyone）。
7. 完成授權後，複製結尾為 `/exec` 的 Web App URL。

## 第 3 步：將 Web App URL 寫入網站
開啟 `index.html`，找到：

`APPS_SCRIPT_URL: "PASTE_APPS_SCRIPT_WEB_APP_URL_HERE"`

將引號內文字換成你的 `/exec` 網址。

## 第 4 步：測試
先使用管理表中的任一戶測試。例如選一戶的「戶號＋驗證碼」。
完成一筆送出後，到 Google Sheets：
- `回覆資料`：應新增一筆 `active` 紀錄。
- `統計`：已回覆戶數應由 0 變成 1。

同一戶再次送出：
- 舊紀錄應改為 `superseded`
- 新紀錄為 `active`
- 統計仍只算 1 戶。

## 管理端查看位置
正式運作後，管理端直接查看 Google Sheets：
- `回覆資料`：逐戶完整回覆內容
- `統計`：整體統計
- `住戶代碼`：戶號、門牌及驗證碼管理資料（不要公開）

## 安全注意
`住戶代碼` 工作表含全社區驗證碼，僅限管理端帳號存取；不要將該工作表或 XLSX 公開分享。
